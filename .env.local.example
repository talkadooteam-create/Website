# Copy this file to .env.local and fill in your Supabase project's values.
# Both are PUBLIC/publishable and safe to expose in the browser.
# The waitlist form inserts { email } into the existing `waitlist` table.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
